<Page
    x:Class="MySongManager.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MySongManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Loaded="Page_Loaded"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView x:Name="filesList"  >
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:MusicProject">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="500" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Margin="10" Grid.Column="0">
                        <TextBlock FontSize="20" Text="{x:Bind ProjectName}" HorizontalAlignment="Left"  />
                            <TextBlock FontSize="14" Text="{x:Bind ProjectPath}" HorizontalAlignment="Left" Opacity="0.5" />
                        </StackPanel>
                      <!--  <TextBlock FontSize="8" Text="{x:Bind Date}" HorizontalAlignment="Center" Grid.Column="1"/> !-->
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            </ListView>

    </Grid>
</Page>
